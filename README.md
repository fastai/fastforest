# fast-forest

A forest that is fast. Requires `cmake`. To install and run a test:

```bash
conda install pybind11
make
```

NB: the makefile is created by cmake, so don't edit it. Instead, edit CMakeLists.txt and run either `make` or `cmake .`.
